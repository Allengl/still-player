export const FINE_TUNE_STEP = 0.1; // seconds (100ms)
export const FADE_DURATION = 3000; // ms
export const FADE_STEPS = 30;
export const FADE_INTERVAL = FADE_DURATION / FADE_STEPS; // 100ms
export const UPDATE_INTERVAL = 50; // ms — player status update interval
export const AUDIO_DIR = 'audio'; // subdirectory under documentDirectory
